using UnityEngine;
using System.Collections;

public class SortingForText : MonoBehaviour {

	// Use this for initialization
    private void Start()
    {
        renderer.renderer.sortingLayerName = "GUI";
    }
}
